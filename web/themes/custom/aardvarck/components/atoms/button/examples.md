    {{ include('aardvarck:button', { element: 'a', label: 'Hello link', variant: 'link' }, with_context = false) }}

    {{ include('aardvarck:button', { element: 'a', label: 'Hello button grey', variant: 'grey' }, with_context = false) }}
    {{ include('aardvarck:button', { element: 'a', label: 'Hello button primary', variant: 'primary' }, with_context = false) }}
    {{ include('aardvarck:button', { element: 'a', label: 'Hello button secondary', variant: 'secondary' }, with_context = false) }}

    {{ include('aardvarck:button', { element: 'button', label: 'Hello button element', variant: 'primary' }, with_context = false) }}

    {{ include('aardvarck:button', { element: 'input', label: 'Hello input element', variant: 'primary' }, with_context = false) }}
